
Object.defineProperty(RegExp, 'escape', { 
    value: function(s) {
        if((typeof s) === 'string')
            return s.replace(/[-\/\\^$*+?.()|[\]{}]/g, '\\$&');
        else
            return s;
    },
    configurable: true,
    writable: true,
    enumerable: false
});
